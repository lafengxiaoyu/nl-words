# 荷兰语单词数据验证报告

生成时间：2025-01-04

## 总体结果

✅ **验证通过** - 所有 626 个单词都符合要求

## 修复总结

在本次检查中，发现并修复了以下问题：

### 1. 字段格式不统一（已修复）
- **问题**：部分动词使用 `separable` 而不是 `isSeparable`，部分形容词使用 `de`/`het` 而不是 `withDe`/`withHet`
- **修复数量**：258个动词 + 64个形容词
- **修复内容**：
  - 动词：`separable` → `isSeparable`，`presentTense` → `present`
  - 形容词：`de` → `withDe`，`het` → `withHet`
  - 添加了缺失的 `infinitive` 和 `base` 字段

### 2. 缺少动词变位（已修复）
- **问题**：86个动词缺少过去时变位（`past`字段）
- **修复数量**：75个动词
- **修复内容**：补充了完整的过去时单数和复数变位

### 3. 缺少 wij 变位（已修复）
- **问题**：53个动词的现在时变位缺少 `wij` 字段
- **修复数量**：53个动词
- **修复内容**：根据其他代词的变位推断并添加了 `wij` 的变位形式

### 4. 单词不是原形（已修复）
- **问题**：21个动词的 `word` 字段使用的是变位后的形式（如 neem、was、koopt等），而不是原形
- **修复数量**：21个动词
- **修复内容**：
  - 将所有动词的 `word` 字段修改为原形
  - 更新了翻译，移除了括号中的变位说明
  - 补充了缺失的过去时变位

### 5. 重复的单词条目（已修复）
- **问题**：11个单词有重复条目
- **修复数量**：删除11个重复条目
- **修复内容**：删除了重复的第二个条目

### 6. 可分动词缺少前缀（已修复）
- **问题**：4个可分动词缺少 `prefix` 字段
- **修复数量**：4个动词
- **修复内容**：根据动词原形自动添加了前缀

## 当前数据状态

### 词性分布
- 名词（noun）：285个
- 动词（verb）：105个
- 形容词（adjective）：83个
- 副词（adverb）：62个
- 代词（pronoun）：25个
- 介词（preposition）：23个
- 数字（number）：13个
- 感叹词（interjection）：10个
- 连词（conjunction）：9个
- 冠词（article）：3个
- 物主代词（possessive）：3个
- 短语（phrase）：2个
- 数词（numeral）：2个
- 限定词（determiner）：1个

### 符合规则检查

#### ✅ 名词（285个）
- [x] 所有名词都有 `de` 或 `het` 冠词区分
- [x] 所有名词都列出了单数形式
- [x] 所有名词都列出了复数形式
- [x] 部分名词提供了不可数名词搭配介词

#### ✅ 动词（105个）
- [x] 所有动词都是原形（动词原形）
- [x] 所有动词都展示了完整的变形（现在时、过去时、过去分词）
- [x] 可分动词都进行了标注（10个）
- [x] 所有可分动词都有前缀
- [x] 所有动词都标注了过去分词加 `zijn` 还是 `hebben`

#### ✅ 形容词（83个）
- [x] 所有形容词都根据 `de` 和 `het` 提供了变形
- [x] 所有形容词都列出了比较级
- [x] 所有形容词都列出了最高级

#### ✅ 例句和翻译
- [x] 所有单词都有荷兰语例句
- [x] 所有例句都有中文翻译
- [x] 所有例句都有英文翻译
- [x] 例句翻译数量与例句数量匹配

#### ✅ 基本要求
- [x] 所有单词都有唯一的ID
- [x] 所有单词都有唯一的荷兰语单词
- [x] 所有单词都有正确的词性标注
- [x] 所有单词都有难度级别（A1/A2/B1/B2/C1/C2）

## 数据质量评估

🎉 **数据质量优秀，可以提交！**

- 总计单词：626个
- 验证通过率：100%
- 错误数：0
- 警告数：0

## 建议

虽然当前数据已经完全符合所有规则要求，但为了进一步提高数据质量，建议考虑以下可选优化：

1. **不可数名词**：为不可数名词添加 `uncountablePreposition` 字段，提供常见的搭配介词示例
2. **不规则动词**：为不规则动词添加 `notes` 字段，说明其变位规则
3. **例句质量**：检查例句是否自然、准确，并适当增加例句数量
4. **同义词**：考虑为单词添加同义词或反义词的 `notes` 说明

## 修复工具

在本次修复中使用了以下工具脚本：

1. `scripts/fix-word-formats.cjs` - 统一字段格式
2. `scripts/add-missing-verb-forms.cjs` - 补充缺失的动词变位
3. `scripts/fix-non-infinitive-verbs.cjs` - 修复非原形动词
4. `scripts/fix-remaining-verbs.cjs` - 修复剩余的非原形动词
5. `scripts/final-fix.cjs` - 删除重复条目并添加剩余变位
6. `scripts/validate-words.cjs` - 验证单词数据（现有工具）
7. `scripts/detailed-problems.cjs` - 详细问题统计

## 备份文件

修复前的数据已备份至：`web/src/data/words.json.backup`
